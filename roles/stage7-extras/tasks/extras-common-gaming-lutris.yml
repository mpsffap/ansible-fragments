---
- name: EXTRAS-COMMON-GAMING-LUTRIS - Ensure keyrings directory exists
  ansible.builtin.file:
    path: "/etc/apt/keyrings"
    state: directory
    mode: "0755"

- name: EXTRAS-COMMON-GAMING-LUTRIS - Download Lutris GPG key and dearmor
  ansible.builtin.shell: |
    curl -fsSL {{ url_lutris_key_url }} | gpg --dearmor | tee {{ url_lutris_key_path }} > /dev/null
  args:
    creates: "{{ url_lutris_key_path }}"

- name: EXTRAS-COMMON-GAMING-LUTRIS - Add Lutris APT repository
  ansible.builtin.apt_repository:
    repo: "deb [signed-by={{ url_lutris_key_path }}] {{ url_lutris_repo }} /"
    filename: "lutris"
    state: present
    update_cache: true

- name: EXTRAS-COMMON-GAMING-LUTRIS - Install Lutris
  ansible.builtin.apt:
    name: lutris
    state: present
