---
- name: Setup base os (Debian)
  hosts: target_hosts
  become: true
  vars_files:
    - "../roles/stage0-init/vars/vars_init.yml"
    - "../roles/stage1-users/vars/vars_user.yml"
    - "../roles/stage2-terminal/vars/vars_installer_terminal.yml"
    - "../roles/stage3-services/vars/vars_installer_services.yml"
    - "../roles/stage4-desktop/vars/vars_installer_desktop.yml"
    - "../roles/stage8-harden/vars/vars_harden.yml"
  roles:
    - stage0-init
    - stage1-users
    - stage2-terminal
    - stage3-services
    - stage4-desktop
    - stage8-harden
