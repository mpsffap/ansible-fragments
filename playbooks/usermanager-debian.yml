---
- name: Manage users (Debian)
  hosts: target_hosts
  become: true
  vars:
    enable_useradd: "true"
    enable_userdel: "true"

  vars_files:
    - "../roles/usermanager-debian/vars/vars.yml"
  roles:
    - usermanager-debian
